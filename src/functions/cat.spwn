extract $

cat = (context){
	extract context

    let curRFile = 0
	if args.length >= 1 {
	    while curFile < args.length{
		print(readfile(args[curRFile]))
		curRFile++
	}
	curFile = 0
    }
}

return @command::new(
	name = 'cat',
	description = 'Displays contents of a file.',
	call = cat,
)
